<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 파일명과 동일하게 -->
<mapper namespace="user">
	<!-- LoginController -->
	<select id="login" resultType="mybatis.vo.UserVO"
	                   parameterType="java.util.Map">
		SELECT * FROM itClips_user
		WHERE id = #{id}
		AND pw = #{pw}
	</select>
	
	<!-- JoinController -->
	<!-- id 중복 검사 -->
	<select id="get_user" parameterType="String" resultType="mybatis.vo.UserVO">
		SELECT * FROM itClips_user
		WHERE id = #{id}
	</select>
	
	<!-- 가입 -->
	<insert id="join" parameterType="mybatis.vo.UserVO">
		INSERT INTO itClips_user(id, pw, nickname, email, birthday, gender, profile_image, join_Date, grade, category)
		VALUES(#{id}, #{pw}, #{nickname}, #{email}, #{birthday}, #{gender}, #{profile_image}, #{join_Date}, #{grade}, #{category})
	</insert>
	
	<!-- 수정 -->
	<!-- 탈퇴 -->
</mapper>